kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: local-es-node-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-es-storage
  resources:
    requests:
      storage: 2Gi
